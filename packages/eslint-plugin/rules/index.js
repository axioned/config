import noConsole from "./no-console.js";

/**
 * All plugin rules. Add new rules here as you create them.
 * Key = rule name (used as `@axioned/no-console` in configs).
 */
const rules = {
  "no-console": noConsole,
};

export default rules;
